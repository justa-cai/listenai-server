# Prometheus configuration for VoxCPM TTS Server monitoring

global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'voxcpm-tts-server'
    static_configs:
      - targets: ['localhost:9090']
    metrics_path: /metrics
